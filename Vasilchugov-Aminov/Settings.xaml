<Window x:Class="Vasilchugov_Aminov.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Vasilchugov_Aminov"
        mc:Ignorable="d"
        Title="Настройки" Height="350" Width="250" Loaded="Window_Loaded">
    <Grid>
        <StackPanel Margin="20,68,20,67">
            <ComboBox Name="change_color" SelectionChanged="change_color_SelectionChanged" Height="20">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="50" Height="50" Fill="{Binding Name}"/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

        </StackPanel>








        <Button ToolTip="Позволяет закрыть окно натсроек" Content="Закрыть" HorizontalAlignment="Center" Margin="0,287,0,0" VerticalAlignment="Top" Height="21" Width="66" Click="Button_Click"/>
        <Label ToolTip="Меняет фон текущего окна" Content="Выбрать цвет фона текущего окна" HorizontalAlignment="Center" Margin="0,27,0,0" VerticalAlignment="Top" Width="210"/>


    </Grid>
</Window>
